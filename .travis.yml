sudo:
  false

language:
  python

python:
  - 2.7
  - 3.3
  - 3.4
  - 3.5

install:
  - pip install --upgrade -r requirements.dev.txt

script:
  - pylama --ignore=E402 gobble
  - py.test --verbose --cov=gobble tests

after_success:
  - python coveralls
