sudo:
  false

language:
  python

python:
  - 2.7
  - 3.3
  - 3.4
  - 3.5

env:
  global:
    - TOXENV="py${PYTHON_VERSION//./}"

install:
  - pip install --upgrade -r requirements.dev.txt

script:
  - pylama gobble
  - PACKAGE=gobble tox

after_success:
  - python coveralls
